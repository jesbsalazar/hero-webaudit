type Props = { lang: "en" | "es" };

/**
 * Process visuals temporarily disabled.
 * The previous generated artwork was removed from the landing page.
 */
export function HeroProcess({ lang: _lang }: Props) {
  return null;
}
